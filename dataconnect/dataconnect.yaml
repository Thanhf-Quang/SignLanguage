specVersion: "v1"
serviceId: "hand-sign-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hand-sign-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
